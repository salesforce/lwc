{
    "private": true,
    "name": "@lwc/playground",
    "version": "2.30.1",
    "type": "module",
    "description": "Playground project to experiment with LWC.",
    "scripts": {
        "dev": "rollup -c --watch",
        "build": "NODE_ENV=production rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "2.30.1",
        "@rollup/plugin-replace": "^5.0.1",
        "lwc": "2.30.1",
        "rollup": "^3.2.5",
        "rollup-plugin-livereload": "^2.0.5",
        "rollup-plugin-serve": "^2.0.1"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "./dist"
                ]
            }
        }
    }
}

{
    "private": true,
    "name": "@lwc/playground",
    "version": "6.6.0",
    "type": "module",
    "description": "Playground project to experiment with LWC.",
    "scripts": {
        "dev": "rollup -c --watch",
        "build": "tsc && NODE_ENV=production rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "6.6.0",
        "@rollup/plugin-replace": "^5.0.5",
        "lwc": "6.6.0",
        "rollup": "^4.16.1",
        "rollup-plugin-livereload": "^2.0.5",
        "rollup-plugin-serve": "^3.0.0"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "{projectRoot}/dist"
                ]
            }
        }
    }
}

{
    "private": true,
    "name": "lwc-playground",
    "version": "2.24.0",
    "description": "Playground project to experiment with LWC.",
    "scripts": {
        "dev": "rollup -c --watch",
        "build": "NODE_ENV=production rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "2.24.0",
        "@rollup/plugin-replace": "^4.0.0",
        "lwc": "2.24.0",
        "rollup": "^2.79.0",
        "rollup-plugin-livereload": "^2.0.5",
        "rollup-plugin-serve": "^2.0.1"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "./dist"
                ]
            }
        }
    }
}

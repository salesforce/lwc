{
    "private": true,
    "name": "@lwc/playground",
    "version": "minor",
    "type": "module",
    "description": "Playground project to experiment with LWC.",
    "scripts": {
        "dev": "rollup -c --watch",
        "build": "NODE_ENV=production rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "minor",
        "@rollup/plugin-replace": "^6.0.2",
        "lwc": "minor",
        "rollup": "^4.40.0",
        "rollup-plugin-livereload": "^2.0.5",
        "rollup-plugin-serve": "^3.0.0"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "{projectRoot}/dist"
                ]
            }
        }
    }
}

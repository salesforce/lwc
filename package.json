{
  "name": "raptor",
  "private": true,
  "description": "Raptor",
  "scripts": {
    "clean": "yarn run clean:packages",
    "lint": "eslint .",
    "test": "lerna run test",
    "build": "lerna run build",
    "build:compiler": "webpack --config scripts/compiler.webpack.js --progress",
    "build:engine": "lerna run build:dist && cp packages/raptor-engine/dist/engine.* dist/engine/",
    "start": "lerna exec yarn start --scope raptor-engine",
    "clean:dist": "rimraf dist/",
    "clean:node_modules": "lerna exec -- rm -rf ./node_modules && rm -rf ./node_modules",
    "clean:packages": "lerna run clean",
    "preinstall": "node -e \"if (process.env.npm_execpath.indexOf('yarn')===-1) throw new Error('npm is not supported, instead use \\\"yarn install\\\"')\"",
    "prepublish": "lerna bootstrap --hoist --concurrency 1 && yarn run build",
    "release": "yarn run prepublish && lerna publish --exact"
  },
  "repository": {
    "type": "git",
    "url": "https://git.soma.salesforce.com/raptor/raptor.git"
  },
  "bin": {
    "raptor-template": "bin/raptor-template-cli.js",
    "raptor-compiler": "bin/raptor-compiler-cli.js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-env": "^1.5.1",
    "babel-preset-flow": "^6.23.0",
    "babili": "^0.0.12",
    "cross-env": "^3.1.4",
    "eslint": "^3.19.0",
    "eslint-plugin-typescript": "^0.1.0",
    "lerna": "2.0.0-rc.4",
    "mocha": "^3.2.0",
    "power-assert": "^1.4.2",
    "rimraf": "^2.6.1",
    "string-replace-webpack-plugin": "^0.1.3",
    "typescript-eslint-parser": "^3.0.0",
    "webpack": "^2.3.3"
  }
}

{
  "name": "frank",
  "version": "1.0.0-alfa",
  "description": "Frankenstein (aka Frank) POC",
  "scripts": {
    "clean": "rimraf fake-cdn/",
    "start": "forever --uid frank fake-server/server.js & rollup -c -w && forever stop frank",
    "build": "npm run clean && rollup -c --environment production",
    "lint": "eslint src/",
    "pretest": "npm run lint",
    "preversion": "npm run clean && npm run build && npm run test",
    "prepublish": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://git.soma.salesforce.com/cpatino/frank.git"
  },
  "author": "Caridy Patiño <cpatino@salesforce.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-es2015-rollup": "^1.1.1",
    "eslint": "^3.0.1",
    "express": "^4.14.0",
    "forever": "^0.15.2",
    "rollup": "^0.34.3",
    "rollup-plugin-babel": "^2.6.1",
    "rollup-plugin-commonjs": "^3.3.1",
    "rollup-plugin-inject": "^1.4.1",
    "rollup-plugin-strip": "^1.1.1",
    "rollup-plugin-typescript": "^0.7.7",
    "rollup-plugin-uglify": "^1.0.1",
    "rollup-watch": "^2.5.0",
    "serve-static": "^1.11.1",
    "typescript-eslint-parser": "eslint/typescript-eslint-parser"
  }
}

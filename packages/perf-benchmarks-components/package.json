{
    "name": "perf-benchmarks-components",
    "version": "2.13.3",
    "private": true,
    "scripts": {
        "build": "rm -fr dist && rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "2.13.3"
    }
}

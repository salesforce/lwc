{
    "name": "perf-benchmarks-components",
    "version": "2.5.7",
    "private": true,
    "scripts": {
        "build": "rm -fr dist && rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "2.5.7"
    }
}

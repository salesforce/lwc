import { getKey, callKey, setKey, deleteKey, inKey, iterableKey, instanceOfKey } from "./methods-noop";

export default {
    getKey,
    callKey,
    setKey,
    deleteKey,
    inKey,
    iterableKey,
    instanceOfKey
};

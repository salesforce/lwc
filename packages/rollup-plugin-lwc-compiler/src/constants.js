/* eslint-env node */

const DEFAULT_NS = 'x';
const DEFAULT_MODE = 'dev';
const DEFAULT_OPTIONS = {
    mode: DEFAULT_MODE,
    resolveFromSource: true,
    resolveFromPackages: true,
};

module.exports = {
    DEFAULT_NS,
    DEFAULT_OPTIONS,
    DEFAULT_MODE
};

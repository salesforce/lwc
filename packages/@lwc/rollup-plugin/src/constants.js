const DEFAULT_MODE = 'dev';
const DEFAULT_OPTIONS = {
    mode: DEFAULT_MODE,
    resolveFromSource: true,
    resolveFromPackages: true,
    sourcemap: false,
};

module.exports = {
    DEFAULT_OPTIONS,
    DEFAULT_MODE
};

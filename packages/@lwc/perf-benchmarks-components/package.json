{
    "name": "@lwc/perf-benchmarks-components",
    "version": "8.21.4",
    "private": true,
    "scripts": {
        "build": "rm -fr dist && rollup -c ./rollup.config.mjs"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "8.21.4"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "{projectRoot}/dist"
                ]
            }
        }
    }
}

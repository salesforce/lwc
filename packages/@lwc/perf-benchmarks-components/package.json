{
    "name": "@lwc/perf-benchmarks-components",
    "version": "7.0.3",
    "private": true,
    "scripts": {
        "build": "rm -fr dist && rollup -c ./rollup.config.mjs"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "7.0.3"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "{projectRoot}/dist"
                ]
            }
        }
    }
}

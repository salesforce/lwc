{
    "name": "@lwc/perf-benchmarks-components",
    "version": "2.29.0",
    "private": true,
    "scripts": {
        "build": "rm -fr dist && rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "2.29.0"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "./dist"
                ]
            }
        }
    }
}

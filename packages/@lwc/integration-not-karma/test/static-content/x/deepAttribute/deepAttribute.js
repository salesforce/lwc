import { LightningElement } from 'lwc';

export default class extends LightningElement {
    one = '1';
    un = { deux: '2' };
    uno = { dos: { tres: '3' } };
    ichi = { ni: { san: { shi: '4' } } };
}

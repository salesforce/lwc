/* eslint-env node */
const DEFAULT_NS = 'x';
const DEFAULT_OPTIONS = { bundle: false, resolveFromPackages: true, resolveFromSource: true };

module.exports = {
    DEFAULT_NS,
    DEFAULT_OPTIONS,
};
